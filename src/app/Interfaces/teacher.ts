import { Data } from "@angular/router";

export interface ITeacher {
    subject_id: number,
    name: string,
    teacher_id: number,
    subject: string,
    description: Text,
    create_at: Date
}